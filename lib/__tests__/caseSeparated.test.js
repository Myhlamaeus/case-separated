const assert = require('assert');
const caseSeparated = require('../index.js');

describe('caseSeparated', () => {
  it('has a test', () => {
    assert(false, 'caseSeparated should have a test');
  });
});
